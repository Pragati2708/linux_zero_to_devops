# lab13 — Question 13

Create the `access.log` file with the provided contents and perform analysis:

- Count how many times the log was accessed (line count)
- Show only lines where status code is 404
- List all unique IPs that accessed the server
- Count how many requests were POST
- List all requested URLs
- Find which IPs triggered 404 errors

Commands to perform the analysis are in `file.txt` and the `access.log` sample is included here.
